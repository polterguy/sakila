/*
 * Automatically generated by Magic
 */

import { throwError } from 'rxjs';
import { Component, Inject } from '@angular/core';
import { MatSnackBar } from '@angular/material/snack-bar';
import { MAT_DIALOG_DATA, MatDialogRef } from '@angular/material/dialog';
import { HttpService } from 'src/app/services/http-service';
import { DialogComponent, DialogData } from '@app/base/dialog.component';

/**
 * Modal dialog for editing your existing Film entity types, and/or
 * creating new entity types of type Film.
 */
@Component({
  templateUrl: './edit.sakila_film.component.html'
})
export class EditSakila_filmComponent extends DialogComponent {

  /**
   * Constructor taking a bunch of services injected using dependency injection.
   */
  constructor(
    public dialogRef: MatDialogRef<EditSakila_filmComponent>,
    @Inject(MAT_DIALOG_DATA) public data: DialogData,
    protected snackBar: MatSnackBar,
    public service: HttpService) {
    super(snackBar);
    this.primaryKeys = ['film_id'];
    this.createColumns = [
      'language_id',
      'original_language_id',
      'title',
      'description',
      'release_year',
      'rating',
      'special_features',
      'rental_duration',
      'rental_rate',
      'length',
      'replacement_cost'
    ];
    this.updateColumns = [
      'language_id',
      'original_language_id',
      'title',
      'description',
      'release_year',
      'rating',
      'special_features',
      'film_id',
      'rental_duration',
      'rental_rate',
      'length',
      'replacement_cost'
    ];
  }

  /**
   * Returns a reference to ths DialogData that was dependency injected
   * into component during creation.
   */
  protected getData() {
    return this.data;
  }
                             
  /**
   * Returns a reference to the update method, to update entity.
   */
  protected getUpdateMethod() {
    return this.service.sakila_film.update(this.data.entity);
  }
  /**
   * Returns a reference to the create method, to create new entities.
   */
  protected getCreateMethod() {
    return this.service.sakila_film.create(this.data.entity);
  }

  /**
   * Closes dialog.
   * 
   * @param data Entity that was created or updated
   */
  public close(data: any) {
    if (data) {
      this.dialogRef.close(data);
    } else {
      this.dialogRef.close();
    }
  }
}
